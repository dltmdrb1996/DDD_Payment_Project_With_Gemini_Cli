refactor: 원시 타입을 값 객체(Value Object)로 전환

- `cardInfo`와 `bankInfo`를 단순 `String`에서 `CardInfo`와 `BankInfo`라는 명시적인 값 객체로 리팩토링했습니다.
- 각 값 객체는 생성 시점에 유효성 검증 로직을 포함하여 도메인 모델의 안전성을 강화합니다.
- 변경된 값 객체를 사용하도록 도메인, 애플리케이션, 테스트 계층의 관련 코드를 모두 수정했습니다.